import React, { Component } from "react";

class Title extends Component {
  render() {
    return (
      <div>
        <h1>Raspberry Pi Tracker</h1>
      </div>
    );
  }
}

export default Title;
